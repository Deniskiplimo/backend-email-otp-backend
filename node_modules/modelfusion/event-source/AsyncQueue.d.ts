export declare class AsyncQueue<T> implements AsyncIterable<T> {
    private queue;
    private resolvers;
    private closed;
    push(value: T): void;
    close(): void;
    [Symbol.asyncIterator](): AsyncIterator<T>;
}
